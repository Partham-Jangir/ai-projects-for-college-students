## Installation

1. Copy `modules/perfex_ai_suite` into your Perfex CRM `modules` directory.
2. Login as admin, go to Setup → Modules, activate `Perfex AI Suite`.
3. The module runs `install.php` to create required tables.
4. Navigate to `AI Suite` menu in admin sidebar.
5. Configure settings as needed.

## Upgrade

1. Replace module files.
2. Run upgrade by visiting module settings; the system triggers `upgrade.php`.
3. Verify database changes.
